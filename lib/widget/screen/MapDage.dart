import 'package:flutter/material.dart';

class MapDage extends StatelessWidget {
  const MapDage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
